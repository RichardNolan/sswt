<%= form_with(model: hamper_item, local: true) do |form| %>
  <% if hamper_item.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(hamper_item.errors.count, "error") %> prohibited this hamper_item from being saved:</h2>

      <ul>
      <% hamper_item.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :hamper_id %>
    <%= form.number_field :hamper_id, id: :hamper_item_hamper_id %>
  </div>

  <div class="field">
    <%= form.label :product_id %>
    <%= form.number_field :product_id, id: :hamper_item_product_id %>
  </div>

  <div class="field">
    <%= form.label :quantity %>
    <%= form.number_field :quantity, id: :hamper_item_quantity %>
  </div>

  <div class="field">
    <%= form.label :price_when_ordered %>
    <%= form.text_field :price_when_ordered, id: :hamper_item_price_when_ordered %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
