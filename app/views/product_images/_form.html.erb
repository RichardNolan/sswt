<%= form_with(model: product_image, local: true) do |form| %>
  <% if product_image.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(product_image.errors.count, "error") %> prohibited this product_image from being saved:</h2>

      <ul>
      <% product_image.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :product_id %>
    <%= form.number_field :product_id, id: :product_image_product_id %>
  </div>

  <div class="field">
    <%= form.label :src %>
    <%= form.text_field :src, id: :product_image_src %>
  </div>

  <div class="field">
    <%= form.label :primary_image %>
    <%= form.check_box :primary_image, id: :product_image_primary_image %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
